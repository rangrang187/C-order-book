# 系统设计标准流程

本文档用于统一系统设计的分析、记录与评审方式。设计的目标不是一次性预测所有细节，而是明确需求和约束、比较关键方案、定义模块契约，并使实现结果可以验证。

使用要求：

- 新系统、核心链路变更、跨模块改造以及性能或可靠性风险较高的功能，应先完成本文档中的相关章节再进入实现。
- 不适用的章节应标记为 `N/A` 并说明原因，不能直接省略。
- 所有性能、容量与可靠性目标都应可量化，并写明统计口径、测试条件和数据来源。
- 重要取舍应记录备选方案和选择理由，避免只保留最终结论。
- 设计文档应随实现和运行事实持续更新；若实现偏离设计，必须同步修改文档或补充决策记录。

---

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 系统/功能名称 |  |
| 文档状态 | 草稿 / 评审中 / 已批准 / 已废弃 |
| 负责人 |  |
| 评审人 | 产品 / 开发 / 测试 / 运维 / 安全等 |
| 创建/更新时间 |  |
| 目标版本 |  |
| 关联资料 | 需求、原型、接口、监控、事故或 ADR 链接 |

同时维护变更记录：

| 日期 | 修改人 | 变更内容 | 变更原因 |
| --- | --- | --- | --- |
|  |  |  |  |

## 1. 系统目标

### 1.1 背景与问题定义

用简短、可验证的语言回答：

- 当前存在什么问题，影响哪些用户或上下游？
- 为什么现在需要解决？不处理会产生什么后果？
- 本次设计希望带来什么业务或工程价值？
- 谁是系统的使用者、维护者和依赖方？

避免把实现方案写成目标。例如，“引入消息队列”是方案，“在突发流量下不丢失已接受的订单”才是目标。

### 1.2 总功能

先用一段话描述系统的整体职责，再列出主要用例和端到端流程。每个用例至少说明：参与者、触发条件、主流程、结果和异常分支。

建议补充一张系统上下文图，展示系统边界、外部参与者和上下游依赖。图中只表达职责与关系，不下沉到类或函数级别。

### 1.3 功能需求（FR）

功能需求应可追踪、可测试，并使用稳定编号。

| ID | 需求描述 | 优先级 | 输入/触发 | 预期输出 | 验收条件 |
| --- | --- | --- | --- | --- | --- |
| FR-001 |  | P0 / P1 / P2 |  |  |  |

检查要点：

- 是否覆盖正常流程、边界条件、重复请求、超时、取消与部分成功？
- 是否定义数据校验、权限校验、幂等性以及结果顺序？
- 是否说明与现有行为的兼容关系？

### 1.4 非功能需求（NFR）

不得只写“高性能”“高可用”或“低延迟”，必须给出数值、统计窗口和环境。

| ID | 维度 | 指标与目标 | 测量口径/条件 | 验收方式 |
| --- | --- | --- | --- | --- |
| NFR-001 | 延迟 | 例如：稳态下端到端 p99 ≤ 100 μs | 并发数、负载、硬件、预热状态 | 基准测试 |
| NFR-002 | 吞吐 | 例如：持续处理 ≥ 500k events/s | 消息大小、读写比例 | 压力测试 |
| NFR-003 | 可用性 | 例如：月度可用性 ≥ 99.99% | 排除项和统计方式 | 监控报表 |
| NFR-004 | 数据正确性 | 例如：已确认数据丢失率为 0 | 故障边界和确认语义 | 故障注入 |

至少评估以下维度：

- 性能：平均值、p50/p95/p99/p99.9、最大可接受值、吞吐和抖动。
- 容量：峰值流量、并发量、数据规模、增长速度和保留周期。
- 可用性与恢复：SLO、RTO、RPO、降级目标和故障域。
- 一致性与正确性：顺序、唯一性、幂等、精度和一致性模型。
- 安全与合规：身份认证、授权、审计、敏感数据和保留要求。
- 可维护性：可观测性、可测试性、发布复杂度和兼容期限。
- 成本：计算、内存、存储、网络以及人力约束。

### 1.5 范围与约束

明确以下内容：

- 范围内：本次必须交付的能力。
- 范围外：明确不解决的问题，防止范围蔓延。
- 假设：设计成立所依赖但尚未完全验证的事实。
- 约束：技术栈、协议、硬件、部署环境、期限、预算和兼容性要求。
- 依赖：外部系统、团队、数据、证书或基础设施。

每个高风险假设都应有验证计划；假设被推翻时，应能定位受影响的设计决策。

### 1.6 成功标准

将 FR、NFR 和业务结果整理成发布验收清单。每项标准都需要负责人、验证方法和证据位置。只有“功能可用”但没有性能、可靠性与运维证据，不能视为设计闭环。

## 2. 负载模型与容量估算

设计前先定义预期负载，至少包含：

- 平均与峰值 QPS/TPS、突发持续时间和日内分布。
- 并发连接数、生产者/消费者数量以及读写比例。
- 单次请求或消息的平均值、p99 大小和最大值。
- 当前数据量、每日增量、保留周期和冷热数据比例。
- 热点分布、分区键分布、流量增长倍数和未来规划周期。
- 正常、峰值、过载和依赖降级四类场景。

估算时保留公式、单位和安全余量，例如：

```text
峰值带宽 = 峰值消息数/秒 × 单条消息字节数 × 协议开销系数
每日新增数据 = 平均写入数/秒 × 86,400 × 单条持久化字节数
所需并发处理数 ≈ 到达率 × 平均处理时间（Little's Law）
计划容量 = 预测峰值 × 增长系数 × 冗余系数
```

估算结果应与压测结果互相校验。无法确认的数据要写成区间，并说明最坏情况对架构的影响。

## 3. 架构设计

架构统一使用 **4+1 视图模型**描述。四个基础视图分别回答系统“做什么、代码如何组织、运行时如何协作、部署在哪里”，场景视图（+1）用于驱动并验证另外四个视图。

4+1 不是五份互相独立的图。所有视图必须使用相同的术语、组件名称和边界，并能从核心场景追踪到实现、运行和部署。

### 3.1 视图总览

| 视图 | 核心问题 | 主要关注者 | 必需产物 |
| --- | --- | --- | --- |
| 场景视图（+1） | 系统如何完成关键用例？ | 产品、架构师、开发、测试 | 用例清单、系统上下文图、关键时序图 |
| 逻辑视图 | 系统提供哪些能力，职责和数据如何划分？ | 架构师、领域专家、开发 | 组件/领域模型、状态及依赖关系 |
| 开发视图 | 源码、模块和构建单元如何组织？ | 开发、测试、构建维护者 | 模块图、代码目录映射、依赖规则 |
| 进程视图 | 运行时单元如何通信、并发和容错？ | 性能、可靠性、开发人员 | 进程/线程模型、运行时序、延迟预算 |
| 物理视图 | 软件部署在哪里，如何扩展和故障切换？ | 运维、SRE、基础设施、安全 | 部署拓扑、资源映射、网络与故障域 |

绘图形式不做强制限制，可以使用 C4、UML、Mermaid 或其他团队工具，但每张图必须注明标题、范围、图例、版本和关键假设。图不能替代文字契约；重要边界、失败语义和约束仍需在正文中说明。

### 3.2 场景视图（+1，驱动视图）

场景视图从 FR、NFR 和主要参与者中选择具有架构代表性的场景，用于发现架构元素并验证四个基础视图是否完整。不要试图把所有业务用例都画成架构场景。

至少选择以下类型：

- 一条最重要的正常端到端流程。
- 一条性能或吞吐压力最大的关键路径。
- 一条包含超时、重复、乱序或部分失败的异常路径。
- 一条启动、关闭、扩缩容、升级或灾难恢复等运维路径。

| 场景 ID | 参与者与触发条件 | 主流程/预期结果 | 关键异常 | 对应 FR/NFR | 优先级 |
| --- | --- | --- | --- | --- | --- |
| SCN-001 |  |  |  |  | P0 / P1 / P2 |

每个 P0 场景都应提供时序图或同等清晰的步骤描述，并标注：

- 系统边界、外部依赖以及请求进入和结果确认的时点。
- 经过的逻辑组件、运行时进程/线程和最终部署节点。
- 数据创建、读取、修改、持久化和转移的位置。
- 同步/异步边界、排队点、超时、重试、背压和降级行为。
- 关键测量点以及相关 NFR 的验证方式。

### 3.3 逻辑视图

逻辑视图描述系统对外提供的能力以及内部职责、核心数据和依赖关系，重点是“系统做什么”，不包含线程、进程和服务器等部署细节。

首先给出系统上下文，说明系统负责什么、不负责什么，以及与用户、数据源、下游服务和基础设施的关系。对每个外部依赖明确协议、所有者、SLA 和失败影响。

随后按领域或稳定业务能力划分组件：

| 逻辑组件 | 核心职责 | 输入 | 输出 | 拥有的数据/状态 | 依赖 | 对应场景 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

逻辑视图必须明确：

- 组件边界、公开契约、依赖方向和禁止跨越的边界。
- 核心实体、值对象、状态机、业务规则和系统不变量。
- 数据的权威来源，以及缓存、副本和派生数据的更新规则。
- 哪些能力是同步提供、异步触发或最终一致。
- 组件在正常和异常场景中的职责，不把错误处理全部推给调用方。

组件应职责单一、避免循环依赖，并能够独立测试和演进。类、成员变量和成员函数的详细设计在第 5 章展开。

### 3.4 开发视图

开发视图描述逻辑组件如何映射到代码仓库、目录、库、可执行文件和构建目标，重点是“软件如何被开发和构建”。

| 逻辑组件 | 仓库/目录 | 模块或构建目标 | 公开接口 | 允许依赖 | 产物 | 负责人 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

开发视图必须说明：

- 代码分层、模块边界、公开/私有头文件和依赖方向。
- 逻辑组件与模块之间是一对一、一对多还是共享映射，以及这样划分的原因。
- 静态库、动态库、可执行文件、生成代码和配置文件等构建产物。
- 第三方依赖、版本管理、替换边界及许可证约束。
- 单元测试、集成测试、基准测试及测试替身所在位置。
- 编译选项、平台差异和 CI 检查如何维护架构边界。

对于 C++ 项目，还应说明 ABI 边界、模板或内联代码的暴露范围、编译时依赖，以及资源所有权规则。不得出现设计图中模块独立、代码中却通过内部头文件相互耦合的情况。

### 3.5 进程视图

进程视图描述运行时的进程、线程、任务、队列和通信关系，重点是并发、顺序、性能、伸缩和故障行为。

| 运行时单元 | 类型与数量 | 核心职责 | 输入/输出 | 状态所有权 | 通信/同步方式 | 过载或故障行为 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 进程 / 线程 / 协程 / 任务 |  |  |  |  |  |

进程视图必须说明：

- 每类进程和线程的创建、职责、数量、生命周期及安全退出顺序。
- 数据由单线程独占、不可变共享、消息传递还是同步原语保护。
- IPC、队列、锁、事件循环和异步回调的边界。
- 顺序保证、幂等、去重、竞争处理、背压和队列容量。
- 超时、重试、取消、熔断、降级、崩溃恢复和状态重建流程。
- 上下文切换、锁竞争、系统调用和排队对尾延迟的影响。

为场景视图中的每条关键路径提供运行时时序，并给出逐段延迟预算：

| 场景/阶段 | 运行时单元 | 目标 p50 | 目标 p99 | 最坏上限 | 测量点 | 超限策略 |
| --- | --- | --- | --- | --- | --- | --- |
| SCN-001 / 接收与解析 |  |  |  |  |  |  |
| SCN-001 / 核心处理 |  |  |  |  |  |  |
| SCN-001 / 持久化或转发 |  |  |  |  |  |  |
| SCN-001 / 合计 |  |  |  |  |  |  |

并发原语、内存序和无锁结构的详细正确性要求在第 6 章展开，专项性能设计在第 7 章展开。

### 3.6 物理视图

物理视图将可执行文件和运行时单元映射到实际基础设施，重点是“系统在哪里运行”以及资源、网络和故障域如何影响系统。

| 部署单元 | 运行时单元/产物 | 节点与故障域 | 副本数 | CPU/内存/磁盘 | 网络/端口 | 扩缩容与故障切换 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

物理视图必须说明：

- 进程、实例、物理机/虚拟机/容器、机架、可用区和机房之间的映射。
- 网络边界、协议、负载均衡、服务发现、防火墙和预期链路延迟。
- 分片、副本、主备或选主机制，以及关键链路是否存在单点故障。
- CPU 核、NUMA 节点和线程亲和性，内存、磁盘与网络队列预算。
- 配置、密钥、时间同步、持久化卷、日志和监控基础设施。
- 扩容、缩容、滚动升级、节点丢失和跨故障域恢复过程。

低延迟系统应明确生产硬件和网络假设；如果基准测试环境与生产拓扑不同，必须说明差异及其对测试结论的影响。

### 3.7 跨视图追踪与一致性

使用追踪矩阵证明每个关键场景都已贯穿四个基础视图：

| 场景 | FR/NFR | 逻辑组件 | 代码模块/产物 | 进程/线程 | 部署节点 | 测试与监控 |
| --- | --- | --- | --- | --- | --- | --- |
| SCN-001 |  |  |  |  |  |  |

评审时重点检查：

- 每个 P0 场景是否能在四个视图中找到完整映射。
- 同一元素在不同视图中的名称、职责、数量和依赖是否一致。
- 逻辑组件是否都有代码归属，运行时单元是否都有构建产物和部署位置。
- 进程视图中的通信是否受到物理网络边界和故障域影响。
- NFR 是否落实到具体运行时机制、资源配置、测试和监控指标。
- 任一视图发生变更时，其他视图和追踪矩阵是否同步更新。

### 3.8 方案比较与决策记录

4+1 视图用于表达架构结果，不能替代方案取舍。关键决策至少比较两个可行方案，不应只论证偏好的方案。

| 方案 | 影响的视图 | 优点 | 缺点/风险 | 性能与成本 | 演进难度 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| A |  |  |  |  |  |  |
| B |  |  |  |  |  |  |

被采纳的关键选择应形成 ADR，记录背景、决策、后果、影响的视图以及何时需要重新评估。

## 4. 接口与数据契约

### 4.1 外部接口

| 接口/事件 | 调用方 → 提供方 | 请求/输入 | 响应/输出 | 超时 | 幂等/重试 | 错误语义 | 版本策略 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |

必须定义：

- 字段类型、单位、范围、必填性、默认值和校验规则。
- 时间戳时区与精度、金额与浮点精度、ID 唯一性和编码格式。
- 向后/向前兼容策略，以及字段新增、废弃和版本迁移规则。
- 超时由哪一层控制，哪些错误可重试，重试是否可能造成重复副作用。
- 背压、限流、最大消息大小和过载时的响应。

### 4.2 数据模型与状态

对核心实体说明：

- 字段、主键、唯一约束、索引、分区方式和生命周期。
- 状态机、合法迁移、触发者、终态和非法迁移处理。
- 数据所有者、读写者、权威来源和缓存一致性策略。
- 创建、更新、归档、删除、备份与恢复规则。
- Schema 演进和历史数据迁移方案。

若数据正确性依赖不变量，应显式列出，例如“同一订单 ID 最多有一个活动订单”。每条不变量都应映射到代码检查、存储约束或测试。

## 5. 模块与类设计

只有在模块边界、数据流和接口稳定后，才进入类与函数级设计。避免用类图代替架构设计。

### 5.1 模块职责

每个模块说明：职责、公开接口、禁止承担的职责、依赖项、线程模型、资源所有权和可测试边界。跨模块只依赖公开契约，不读取对方私有状态。

### 5.2 成员变量

| 名称 | 类型 | 含义/单位 | 所有权与生命周期 | 可变性 | 并发访问规则 | 不变量 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

设计准则：

- 使用明确的类型表达业务语义，避免用裸整数混合表示价格、数量、时间和 ID。
- 明确资源所有权；优先值语义和 RAII，非拥有型指针/引用必须有可证明的生命周期。
- 成员默认私有，通过最小公开接口维护不变量。
- 能在构造时确定的状态不要延迟初始化；不可变状态声明为 `const`。
- 明确共享状态由哪个线程写、哪些线程读，以及同步手段。
- 高频路径考虑对象大小、内存布局、缓存局部性、对齐和伪共享；必须用测量结果支持非常规优化。
- 避免重复存储可推导状态；如为性能缓存派生值，应定义失效和一致性规则。

### 5.3 成员函数

| 函数/签名 | 职责 | 前置条件 | 后置条件 | 失败/异常 | 线程安全 | 复杂度/延迟预算 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

设计准则：

- 一个函数完成一个清晰动作，名称应表达业务意图而非实现细节。
- 明确输入输出、前置条件、后置条件和对对象状态的影响。
- 查询函数尽量无副作用；能标记为 `const`、`noexcept`、`[[nodiscard]]` 的接口应明确表达契约。
- 高频路径避免未受控的内存分配、阻塞 I/O、锁竞争、系统调用、异常抛出和不可预测递归。
- 返回错误码、结果类型还是异常应保持一致，并定义调用方必须处理的失败类别。
- 不向外暴露内部容器的可变引用，不让调用方绕过类不变量。
- 对算法写明时间/空间复杂度；关键函数写明基准测试与性能回归阈值。

## 6. 并发与执行模型

必须明确：

- 使用单线程事件循环、线程池、流水线、actor、生产者/消费者还是混合模型，为什么。
- 每类线程的职责、数量、创建与关闭顺序，以及 CPU/NUMA 亲和性需求。
- 数据所有权是线程独占、不可变共享、消息传递还是受锁保护。
- 同步原语、锁顺序、临界区、内存序和无锁结构的正确性依据。
- 队列容量、满/空行为、背压传播、丢弃策略和公平性。
- 事件顺序、跨分区顺序、重复处理和并发冲突规则。

并发设计评审必须覆盖数据竞争、死锁、活锁、饥饿、优先级反转、伪共享和安全退出。无锁不等于无等待，也不必然更快；采用无锁算法时，应说明进度保证、ABA/内存回收策略并提供压力测试证据。

## 7. 性能与低延迟设计

先定义基线和预算，再选择优化手段。性能设计至少覆盖：

- 热路径和非热路径的边界，以及每条热路径的调用链。
- CPU 周期、缓存未命中、分支、内存分配、锁、系统调用、上下文切换和网络往返预算。
- 是否需要预分配、对象池、批处理、零拷贝、固定容量容器或定制分配器。
- 批量处理带来的吞吐提升与排队延迟之间的取舍。
- 数据结构、内存布局和算法在真实数据分布下的表现。
- 冷启动、预热、长时间运行、突发流量和资源耗尽时的尾延迟。

优化规则：

1. 使用端到端指标定位问题，再用 profiler、硬件计数器或跟踪数据定位原因。
2. 基准测试采用接近生产的编译选项、硬件、数据集和并发模型。
3. 同时报告分位数、吞吐、错误率和资源占用，不只报告平均耗时。
4. 每项复杂优化都应保留正确性测试、性能对比和回退方式。
5. 不以牺牲正确性、可恢复性和可观测性换取未经证实的性能提升。

## 8. 可靠性与故障处理

建立故障清单，并逐项说明检测、隔离、恢复和数据影响。

| 故障场景 | 检测方式 | 系统行为 | 数据影响 | 恢复方式 | 验证方法 |
| --- | --- | --- | --- | --- | --- |
| 进程崩溃 |  |  |  |  |  |
| 依赖超时/不可用 |  |  |  |  |  |
| 网络分区/乱序/重复 |  |  |  |  |  |
| 存储满或损坏 |  |  |  |  |  |
| 流量或队列过载 |  |  |  |  |  |

同时明确：

- 超时、重试、指数退避、熔断、隔离、限流和降级策略。
- 幂等键、去重窗口、确认语义和“未知结果”的处理。
- 快照、日志、备份、重放、故障切换和人工处置流程。
- 启动、关闭和崩溃恢复期间如何维护数据不变量。
- 每种降级模式牺牲什么、保留什么，以及退出降级的条件。

## 9. 可观测性与运维

可观测性应服务于 SLO、故障定位和容量规划，而不是简单增加日志。

- 指标：请求量、错误率、延迟分位数、队列深度、丢弃数、资源利用率和业务正确性指标。
- 日志：结构化字段、级别、采样、关联 ID、敏感信息脱敏和保留周期。
- 跟踪：跨组件关键路径、采样策略和额外开销上限。
- 告警：触发条件、持续时间、严重级别、责任人、处置手册和自动恢复动作。
- 运维：健康检查、就绪检查、诊断接口、配置变更、容量扩展和灾难恢复演练。

每个 NFR 应至少对应一个可持续观测的指标。监控本身不得显著破坏低延迟目标，热路径上的埋点开销也必须测量。

## 10. 安全与合规

根据威胁模型评估：

- 身份认证、最小权限授权和服务间身份。
- 传输中与静态数据加密、密钥轮换和凭据管理。
- 输入校验、防重放、限流、审计和安全事件追踪。
- 敏感数据分类、脱敏、访问记录、保留与删除策略。
- 第三方依赖、供应链、漏洞修复和许可证风险。

安全控制若会增加关键路径延迟，应计入预算并优化实现，不能直接绕过。

## 11. 发布、迁移与回滚

发布方案至少说明：

- 功能开关、灰度/金丝雀范围、观察窗口和放量条件。
- 配置、协议、Schema 和历史数据的迁移顺序。
- 新旧版本混合运行时的兼容行为。
- 回滚触发条件、步骤、负责人和数据回滚限制。
- 发布前检查、发布中监控和发布后验收。

数据库或协议变更优先采用“先扩展、后迁移、再收缩”的兼容方式。任何不可逆步骤都要显式标记，并在执行前完成备份和恢复演练。

## 12. 测试与验证计划

| 需求/风险 | 测试类型 | 场景与数据 | 通过标准 | 环境 | 证据 |
| --- | --- | --- | --- | --- | --- |
| FR/NFR 编号 | 单元/集成/端到端/性能/故障注入 |  |  |  |  |

测试范围包括：

- 单元测试：算法、状态机、边界值、不变量和错误分支。
- 集成/契约测试：协议、Schema、依赖错误和版本兼容。
- 端到端测试：核心用户流程和完整数据链路。
- 并发测试：竞态、顺序、重复、压力下关闭与恢复；必要时使用 sanitizer。
- 性能测试：微基准、端到端压测、容量上限、长稳测试和性能回归。
- 韧性测试：进程崩溃、网络故障、依赖降级、磁盘满和资源耗尽。
- 安全测试：权限边界、恶意输入、凭据泄漏和依赖扫描。

基准测试结果必须可复现：记录代码版本、编译器与参数、硬件、操作系统、CPU 频率策略、数据集、预热方式、运行次数和原始结果。

## 13. 风险与待办

| ID | 风险/未知项 | 概率 | 影响 | 缓解措施或验证实验 | 负责人 | 截止日期 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 |  | 高/中/低 | 高/中/低 |  |  |  |  |

设计评审前应关闭会改变总体架构的未知项。可以延后的小问题必须有负责人和期限，不能只写 `TODO`。

## 14. 设计评审门禁

### 14.1 进入评审前

- [ ] 背景、范围、假设和依赖已明确。
- [ ] FR 与 NFR 已编号、量化并具备验收方法。
- [ ] 负载模型和容量估算有数据来源及安全余量。
- [ ] 4+1 架构视图完整，P0 场景已贯穿四个基础视图并建立追踪关系。
- [ ] 架构边界、关键流程、状态和接口契约完整且跨视图一致。
- [ ] 关键方案已比较，选择理由和代价已记录。
- [ ] 并发、故障、安全、可观测性和发布方案已覆盖。
- [ ] 高风险项已有原型、实验结果或明确验证计划。
- [ ] 图、表、术语和正文一致，无失效链接或未解释缩写。

### 14.2 评审结论

评审只能给出以下结论之一：

- **通过**：可进入实现，遗留项不影响核心方案。
- **有条件通过**：列出必须在指定阶段前关闭的事项、负责人和日期。
- **不通过**：核心需求、风险或方案依据不足，需要重新设计并再次评审。

评审意见必须转为可追踪事项；口头结论不作为设计批准依据。

### 14.3 实现完成后

- [ ] 实现与设计一致，偏差已形成更新或 ADR。
- [ ] 所有 FR、NFR 都有测试或运行证据。
- [ ] 性能结果满足延迟预算，且无不可接受的尾延迟回退。
- [ ] 监控、告警、操作手册、回滚和恢复流程可用。
- [ ] 风险项已关闭或被系统负责人明确接受。
- [ ] 文档已更新为当前真实状态并指定后续维护人。

## 15. 最佳实践

1. **先问目标，再谈技术。** 架构选择必须能追溯到需求、约束或已验证的风险。
2. **先定义契约，再并行实现。** 清晰的接口、状态和错误语义比共享实现细节更利于协作。
3. **用数据做取舍。** 容量、性能和成本以估算、实验和生产指标为依据。
4. **为尾部情况设计。** 峰值、慢依赖、重复、乱序、部分失败和恢复过程往往决定系统上限。
5. **使失败可控。** 设置资源上限、背压、超时和降级，避免无界队列、无限重试和级联故障。
6. **保持可演进。** 协议、数据和部署变更应支持兼容迁移与安全回滚。
7. **把可观测性视为契约。** 设计阶段就确定如何证明系统正确、快速和可用。
8. **优化关键路径，不扩散复杂度。** 将低延迟专用实现限制在经测量的热路径内。
9. **文档记录“为什么”。** 代码通常能说明“怎么做”，设计文档应保留背景、替代方案和后果。
10. **完成反馈闭环。** 发布后的真实负载、性能、故障和运维经验应回写到设计与标准中。

---

## 附录：推荐设计顺序

```text
问题与目标
  → FR / NFR / 范围
  → 负载与容量模型
  → 场景视图（+1）与系统边界
  → 逻辑 / 开发 / 进程 / 物理视图
  → 跨视图追踪、方案比较与决策
  → 接口、数据和并发契约
  → 类、成员变量与成员函数
  → 故障、安全、可观测性和发布方案
  → 测试与验证计划
  → 设计评审
  → 实现、验证、发布和反馈
```

原则上按上述顺序推进；原型和基准测试可以提前进行，用于验证会影响架构的关键假设。
